/* eslint-disable-next-line */
import { View, Text } from 'react-native';

export const Icon = () => {
  return (
    <View>
      <Text>Add Component here</Text>
    </View>
  );
};

export default Icon;
